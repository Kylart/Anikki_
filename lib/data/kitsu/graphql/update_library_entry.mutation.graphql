# Write your query or mutation here
mutation KitsuUpdateLibraryEntry($progress: LibraryEntryUpdateProgressByMediaInput!) {
	libraryEntry {
		updateProgressByMedia(input: $progress) {
      libraryEntry {
        ...kitsuLibraryEntry
      }
			errors {
				message
				code
				path
			}
		}
	}
}
